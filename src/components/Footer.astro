---
// Footer principal
---

<footer class="footer">
  <div class="container">
    <div class="footer-content">
      <div class="footer-brand">
        <h3 class="footer-logo">Helenn Lab</h3>
        <p class="footer-tagline">Création de sites web professionnels, modernes et performants.</p>
      </div>
      <div class="footer-links">
        <nav aria-label="Navigation footer">
          <ul class="footer-nav">
            <li><a href="/">Nous</a></li>
            <li><a href="/contact">Contact</a></li>
          </ul>
        </nav>
      </div>
    </div>
    <div class="footer-bottom">
      <p>&copy; {new Date().getFullYear()} Helenn Lab. Tous droits réservés.</p>
    </div>
  </div>
</footer>

<style>
  .footer {
    background-color: var(--bg-dark);
    color: var(--text);
    padding: var(--spacing-lg) 0 var(--spacing-md);
    margin-top: var(--spacing-2xl);
  }

  .footer-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--spacing-lg);
    margin-bottom: var(--spacing-md);
  }

  .footer-logo {
    font-size: 1.5rem;
    margin-bottom: var(--spacing-sm);
    color: var(--text);
  }

  .footer-tagline {
    color: var(--text-muted);
    max-width: 300px;
  }

  .footer-nav {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-sm);
    list-style: none;
  }

  .footer-nav a {
    color: var(--text-muted);
    transition: color var(--transition-fast);
  }

  .footer-nav a:hover {
    color: var(--text);
  }

  .footer-bottom {
    padding-top: var(--spacing-md);
    border-top: 1px solid var(--border);
    text-align: center;
    color: var(--text-secondary);
    font-size: 0.875rem;
  }

  @media (max-width: 768px) {
    .footer-content {
      grid-template-columns: 1fr;
      gap: var(--spacing-md);
    }
  }
</style>
